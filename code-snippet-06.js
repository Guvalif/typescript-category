/*
    Code Snippet 06
*/

$('#first')                 // id ::= 'first' であるDOM要素を捕捉し...
    .next()                 // 隣接DOM要素へ移動し...
    .next()                 // 隣接DOM要素へ移動し...
    .next()                 // 隣接DOM要素へ移動し...
    .end()                  // 移動を1つキャンセルし...
    .end()                  // 移動を1つキャンセルし...
    .addClass('highlight'); // 現在捕捉しているDOM要素 ::= '<li>2</li>' に 'highlight' クラスを付与する